"""poc.src package.

This is a lightweight package initializer. Keep it minimal to avoid
import-time side effects. Prefer importing subpackages explicitly, e.g.

    from poc.src import classification

Public symbols are intentionally small so importing ``poc.src`` is cheap.
"""

# package version (update as needed)
__version__ = "0.1.0"
